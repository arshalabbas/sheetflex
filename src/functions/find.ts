import Driver from "../driver";

const find = (driver: Driver, collectionName: string, error: any, predicate) => {
  return new Promise(async (resolve, reject) => {
    // TODO: filter with the predicate that user provided
  })
}

export default find;